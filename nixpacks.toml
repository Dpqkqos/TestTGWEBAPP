[phases.setup]
nixPkgs = ["python310", "pip", "sqlite"]

[phases.install]
commands = [
    "python -m pip install --upgrade pip",
    "pip install -r bot/requirements.txt",
    "pip install -r webapp/requirements.txt"
]

[start]
commands = [
    "python bot/bot.py & python webapp/app.py"
]